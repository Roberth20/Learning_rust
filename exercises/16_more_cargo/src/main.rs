/// Code documentation is done with three /, this will render as HTML
/// and is the syntax is markdown, we can create the documentation running
/// `cargo doc`

fn main() {
    println!("Hello, world!");
}
